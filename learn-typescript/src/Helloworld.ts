function printStatusCode(code: string | number) {
    console.log(`My status code is ${(code as string).toUpperCase()}.`)
  }
let message:string|number = "HelloWorld";
console.log(message.toUpperCase());